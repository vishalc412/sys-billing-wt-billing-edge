# sys-billing migration report

Generated by `generate:all`. Source: `archive/sapi-billing/` (Mule 4 `sapi-billing`, imported into
this repo by the user before this run; **left untouched** — no files under `archive/` were
modified). Target: this repo's `src/`.

## Before you read further: repo-vs-CLAUDE.md mismatch found at the start of this run

This repo's `CLAUDE.md` describes an already-existing, fully-built feature-sliced Spring Boot app
(primaryAccount/pastDueToday/pendingCancellation/externalPrimaryAccount packages, SOAP client,
SAML, etc.). At the start of this run the actual repo contained only the bare Spring Initializr
skeleton (`pom.xml` with just `spring-boot-starter`, one `SysBillingApplication.java`, no
`archive/`, `kustomize/`, or `schemas/`). The Mule source also wasn't in the repo; the user copied
it in from a sibling folder on their machine mid-session. Everything below was therefore a
from-scratch build against the real Mule flows, using CLAUDE.md as the intended target
architecture/conventions (which it describes accurately) rather than as a description of prior
code (which didn't exist).

No `schemas/BillingService.wsdl` exists in the Mule source, so there is no generated
`com.westfieldgrp.billing` JAX-WS/JAXB client package in this build — SOAP request/response beans
are hand-written JAXB classes under each feature's `entities/` package, matching the fields
actually observed in the Mule DataWeave scripts and RAML examples.

## 1. pom.xml

- Parent stayed at `spring-boot-starter-parent:4.1.0` (already set in the skeleton pom — this is
  newer than the `3.5.14` CLAUDE.md describes; kept as-is rather than downgraded since it was
  already committed and resolves cleanly). Spring Boot 4.1 pulls in Spring Framework 7 / Spring
  Security 7.
- Added: spring-boot-starter-web, -validation, -security, -oauth2-resource-server, -cache,
  -actuator; caffeine; resilience4j-spring-boot3 (2.3.0, pinned — not in the Spring Boot 4.1 BOM);
  micrometer-tracing-bridge-otel + opentelemetry-exporter-otlp + micrometer-registry-otlp;
  jakarta.xml.bind-api + jaxb-runtime (4.0.9, pinned — JAXB isn't bundled since Java 11); commons-text
  (StringSubstitutor for SOAP/SAML templates); springdoc-openapi-starter-webmvc-ui (2.8.10, pinned);
  lombok; spring-security-test.
- **Deviation**: `spring-boot-starter-aop` does not exist in the Spring Boot 4.1 BOM (dropped
  between 3.5 and 4.1 — likely folded into core). Left it out; Resilience4j's Spring proxy-based
  `@CircuitBreaker`/`@Retry` only need `spring-context`'s built-in AOP support, which is already a
  transitive dependency, so nothing extra was needed once the missing artifact was removed.
- Fixed the JaCoCo exclude mismatch CLAUDE.md flagged: excludes now use `**/dtos/request/**` and
  `**/dtos/response/**` (plural, matching the real package names) instead of `**/dto/**`.
- groupId was left as the existing `com.westfiled.api` (typo, matches the folder name on disk) —
  not renamed, out of scope for this run.

## 2. Config conversion

See `src/main/resources/application.yaml` + `application-{dev,test,stage,perf,local,prod}.yaml`
and `kustomize/`. Full property mapping table below.

## 3. Code conversion

## 4. Tests

## 5. Docker / Kustomize

## 6. Build

## needs_input / manual follow-ups
