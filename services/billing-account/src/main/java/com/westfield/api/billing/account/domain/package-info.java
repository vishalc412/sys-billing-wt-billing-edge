/**
 * domain
 *
 * <p>Entities, value objects and the business rules extracted from DataWeave. No Spring, no JAXB, no DTOs.
 *
 * <p>Module scope: CAP-005, CAP-006, CAP-007, CAP-008 - the ESB-backed /primaryAccount family. Owned by exactly one work packet (ADR-0001); no other agent writes here.
 */
package com.westfield.api.billing.account.domain;
