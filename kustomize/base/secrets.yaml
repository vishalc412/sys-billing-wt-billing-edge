# Placeholder secret manifest. Never commit real values here — populate via your
# secrets manager / CI pipeline substitution before apply, or let an environment
# overlay's SealedSecret / ExternalSecret replace this resource entirely.
apiVersion: v1
kind: Secret
metadata:
  name: billing-secrets
type: Opaque
stringData:
  billing-saml-username: "{{ BILLING_SAML_USERNAME }}"
  billing-saml-password: "{{ BILLING_SAML_PASSWORD }}"
  truststore-password: "{{ TRUSTSTORE_PASSWORD }}"
  oauth-issuer-uri: "{{ OAUTH_ISSUER_URI }}"
