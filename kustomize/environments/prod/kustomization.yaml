apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: prod-
resources:
  - ../../base
patches:
  - path: deployment.patch.yaml
configMapGenerator:
  - name: billing-config
    behavior: merge
    literals:
      - RESPONSE_TIMEOUT_MS=20000
      - SAML_WSA_ADDRESS=urn:sts:mulesoft:unt:to:saml:prod
